version = 1.0

description ="""
Example project for a Gradle build

Project name = ${project.name}

More detailed information here...

"""

allprojects {
    apply plugin: 'java'
    group = 'org.gradle.sample'
    version = '1.0'
    sourceCompatibility = java_version

//    dependencies{
//        testCompile 'junit:junit:4.11'
//        //compile 'log4j:log4j:1.2.17'
//        compile 'org.apache.logging.log4j:log4j-api:2.5'
//        compile 'org.apache.logging.log4j:log4j-core:2.5'
//    }
}

subprojects {
    apply plugin: 'java'
    group = 'SmartFinance'
    repositories {
        mavenCentral()
    }

    dependencies{
        //testCompile 'junit:junit:4.8.2'
        testCompile 'junit:junit:4.11'
//        compile 'log4j:log4j:1.2.17'
        compile 'org.apache.logging.log4j:log4j-api:2.5'
        compile 'org.apache.logging.log4j:log4j-core:2.5'
    }

    //http://stackoverflow.com/questions/20136214/gradle-common-resource-dependency-for-multiple-java-projects
    sourceSets.main.resources.srcDir "../src/main/resources";
//    sourceSets.test.resources.srcDir "../src/test/resources"
    sourceSets.test.resources.srcDir "../src/main/resources"

    test {
        testLogging {
            //http://mrhaki.blogspot.ca/2014/10/gradle-goodness-show-standard-out-or.html
            // Make sure output from
            // standard out or error is shown
            // in Gradle output.
            showStandardStreams = true
        }
    }

    configurations.all {
        // Check for updates every build
        resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
    }

    project(':database_layer') {
        dependencies {
            compile project(':database_engine') /*{changing = true}*/
            compile 'org.hibernate:hibernate-entitymanager:'+hibernate_version //JPA
            compile 'org.hibernate:hibernate-java8:'+hibernate_version //Java8 specific data-types such as any of the JSR 310 Date/Time types in domain model
            compile 'org.hibernate:hibernate-c3p0:'+hibernate_version //For connection pooling
        }
    }
}




//test {
//    testLogging {
//        //http://mrhaki.blogspot.ca/2014/10/gradle-goodness-show-standard-out-or.html
//        // Make sure output from
//        // standard out or error is shown
//        // in Gradle output.
//        showStandardStreams = true
//    }
////    dependencies{
//////    compile group: 'org.hibernate', name: 'hibernate-core', version: hibernate_version
////
////        testCompile 'junit:junit:4.8.2'
////        test project(':database')
////        test project(':import_old_data')
////    }
//}

//dependencies{
////    compile group: 'org.hibernate', name: 'hibernate-core', version: hibernate_version
//
//    testCompile 'junit:junit:4.8.2'
//}
